import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'converte-bitcoin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
